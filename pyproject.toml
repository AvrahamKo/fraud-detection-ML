[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "fraud-detection-ml"
version = "0.1.0"
description = "E-Commerce Fraud Decisioning & Analytics Engine"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]
